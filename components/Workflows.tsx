"use client"; import {useState} from "react";
const prompt=[['Initial prompt','Define the goal and constraints.'],['Model output','Inspect the response behavior.'],['Identify failure','Locate the precise failure mode.'],['Prompt revision','Adjust instruction and context.'],['Retest','Run the revised prompt again.'],['Validate','Check against success criteria.']];
export default function Workflows(){const [active,setActive]=useState(0);return <><section className="workflow"><div className="section-head"><span>06 / PROMPT ENGINEERING</span><h2>Iteration over<br/><em>incantation.</em></h2></div><div className="prompt-work"><div className="prompt-steps">{prompt.map((p,i)=><button className={active===i?'active':''} onClick={()=>setActive(i)} key={p[0]}><i>{String(i+1).padStart(2,'0')}</i><b>{p[0]}</b><span>→</span></button>)}</div><div className="prompt-detail"><span>STEP {String(active+1).padStart(2,'0')}</span><h3>{prompt[active][0]}</h3><p>{prompt[active][1]}</p><div className="terminal"><i/><i/><i/><pre>{`workflow.stage(${active+1})\n  → inspect()\n  → evaluate(criteria)\n  → ${active===5?'approved':'iterate'}`}</pre></div></div></div></section><section className="architectures"><div><span className="eyebrow">RAG ARCHITECTURE</span><h3>Retrieve what matters.<br/><em>Then generate.</em></h3><div className="arch-row">{["Documents","Chunking","Embeddings","Vector DB","Retrieval","LLM","Answer"].map(x=><span key={x}>{x}</span>)}</div><p>Built with FAISS, Sentence-Transformers and Qdrant.</p></div><div><span className="eyebrow">AGENTIC WORKFLOW</span><h3>Orchestrate. Handoff.<br/><em>Validate.</em></h3><div className="agent-map"><span>USER</span><b>↓</b><span>PLANNER / AGENT</span><b>↓</b><div><i>TOOL</i><i>RETRIEVAL</i><i>REASONING</i></div><b>↓</b><span className="valid">VALIDATION</span><b>↓</b><span>RESPONSE</span></div></div></section></>}
