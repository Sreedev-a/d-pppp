"use client"; import {useState} from "react"; import {projects} from "@/data/profile";
export default function Projects(){const [selected,setSelected]=useState<(typeof projects)[0]|null>(null);return <section id="projects"><div className="section-head split"><div><span>04 / SELECTED PROJECTS</span><h2>Systems designed to be<br/><em>examined.</em></h2></div><p>From agentic pipelines to computer vision—each project pairs implementation with a clear way to inspect system behavior.</p></div><div className="projects">{projects.map((p,i)=><article className={`project ${i===0?'featured':''}`} key={p.number}><div className="project-info"><span className="project-no">PROJECT / {p.number}</span><h3>{p.title}</h3><time>{p.date}</time><p>{p.description}</p><div className="tags">{p.tech.map(t=><span key={t}>{t}</span>)}</div><button className="explore" onClick={()=>setSelected(p)}>Explore project <b>↗</b></button></div><div className={`project-visual ${p.accent}`}><span className="visual-label">SYSTEM ARCHITECTURE</span><div className="pipeline">{p.flow.map((f,j)=><div className={f==="Validation"?"validate":""} key={f}><span>{String(j+1).padStart(2,'0')}</span><b>{f}</b>{j<p.flow.length-1&&<i>→</i>}</div>)}</div><div className="scanline"/></div></article>)}</div>{selected&&<div className="modal-backdrop" onMouseDown={()=>setSelected(null)}><div className="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title" onMouseDown={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setSelected(null)} aria-label="Close project details">×</button><span>PROJECT / {selected.number}</span><h2 id="modal-title">{selected.title}</h2><p className="modal-lede">{selected.description}</p><div className="modal-grid"><div><h3>Architecture</h3><div className="modal-flow">{selected.flow.map(x=><span key={x}>{x}</span>)}</div></div><div><h3>Key contribution</h3><p>{selected.contribution}</p><h3>Evaluation approach</h3><p>{selected.evaluation}</p></div></div><div className="tags">{selected.tech.map(t=><span key={t}>{t}</span>)}</div></div></div>}</section>}
