"use client"; import {useState} from "react"; import {skills} from "@/data/profile";
export default function Skills(){const cats=Object.keys(skills) as (keyof typeof skills)[],[active,setActive]=useState(cats[0]);return <section id="skills"><div className="section-head"><span>03 / CAPABILITIES</span><h2>A focused technical<br/><em>toolkit</em></h2></div><div className="skill-ui"><div className="skill-tabs" role="tablist">{cats.map((c,i)=><button role="tab" aria-selected={active===c} onClick={()=>setActive(c)} key={c}><span>0{i+1}</span>{c}<b>→</b></button>)}</div><div className="skill-cloud" role="tabpanel"><span className="skill-label">SELECTED CAPABILITIES / {active.toUpperCase()}</span>{skills[active].map((s,i)=><div style={{animationDelay:`${i*.035}s`}} key={s}>{s}<i>{String(i+1).padStart(2,'0')}</i></div>)}</div></div></section>}
